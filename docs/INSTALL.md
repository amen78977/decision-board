# التركيب

## Claude Code — بلَغن (موصى به)

```
/plugin marketplace add amen78977/decision-board
/plugin install decision-board@decision-board
```

**تركيب محلي (قبل النشر):** من جذر المستودع — لاحظ `./` لا `.`:

```bash
claude plugin marketplace add ./
claude plugin install decision-board@decision-board
claude plugin details decision-board   # يجب أن يظهر Agents (5)
```
```

تحقق: `/plugin` ثم تبويب Installed · وللوكلاء: `/agents`

جرّب: `/decide هل أوظّف أول موظف الآن؟`

---

## Claude Code — بلا بلَغن

```bash
git clone https://github.com/amen78977/decision-board.git
cd decision-board

mkdir -p ~/.claude/agents && cp agents/*.md ~/.claude/agents/
mkdir -p ~/.claude/skills && cp -r skills/decision-board ~/.claude/skills/
```

⚠️ إن لم يكن مجلد `agents` موجوداً قبل بدء الجلسة — أعد التشغيل مرة واحدة.

---

## Claude Code — لصق واحد

الصق محتوى [`standalone/AGENT.md`](../standalone/AGENT.md) في المحادثة. سيجلب الملفات ويثبّتها بنفسه.

---

## Gemini CLI

راجع [`GEMINI.md`](GEMINI.md)

---

## دردشة عادية (ChatGPT · Claude.ai · غيرها)

الصق محتوى [`standalone/CHAT.md`](../standalone/CHAT.md) في بداية المحادثة أو في التعليمات المخصصة.

⚠️ العزل هنا تسلسلي داخل سياق واحد، لا عزل حقيقي.

---

## بعد التركيب — اختبار القبول

| # | الاختبار | النجاح |
|---|---|---|
| ١ | اسأل سؤالاً تافهاً | لا يشغّل الطاولة (مستوى ١) |
| ٢ | اعرض قراراً تعرف أنه سيئ | يعترض بوضوح، لا يجامل |
| ٣ | اعرض قراراً متيناً | يقول إنه متين ولا يفتعل اعتراضاً |
| ٤ | اعرض قراراً بصياغة متحمسة | لا يعكس حماسك |
| ٥ | راجع أي رد | سطر `📋` موجود في المستويين ٢ و٣ |

**إن سقط اختبار ٢ أو ٣ — المنظومة لا تعمل.** راجع أن ملف `opponent.md` نُسخ حرفياً بلا تخفيف.

---

## استكشاف الأخطاء

| العطل | السبب الغالب |
|---|---|
| الوكلاء لا يظهرون في `/agents` | مجلد `agents` أُنشئ بعد بدء الجلسة — أعد التشغيل |
| يجاوب دائماً بلا استشارة أحد | البوابة تصنّف كل شيء مستوى ١ — تحقق من `diagnostician.md` |
| المعارض يجامل | الملف اختُصر عند النسخ — أعد جلبه حرفياً |
| الرد طويل جداً | قيد الـ١٥ سطراً غائب من ملف التعليمات |
| سطر `📋` مفقود | التعليمات لم تُكتب في `CLAUDE.md` |

---

## الاختبار

**١. فحص بنيوي — بلا كلفة، يعمل في CI:**

```bash
bash scripts/validate.sh
```

يفحص البنية · صحة JSON · frontmatter الوكلاء الخمسة · تطابق الاسم مع اسم الملف · العناصر النائبة · **نشر القواعد الـ١٢ على المشتقات الثلاثة** · وتسريب أسماء الوكلاء في سطر `📋`.

**٢. فحص المخطط الرسمي:**

```bash
claude plugin validate ./ --strict
```

**٣. اختبار سلوكي ميكانيكي** (يتطلب تثبيت البلَغن وجلسة مصادَقة):

```bash
bash scripts/smoke.sh
```

يشغّل الحالات الأربع ويفحص ما يُحسم آلياً: غياب `📋` في المستوى ١ · وجوده في ٢ و٣ · حد الـ١٥ سطراً · غياب عبارات التمييع · عدم تسريب أسماء الوكلاء.

**٤. تقييم سلوكي بحَكَم نموذجي** — الأحكام التي لا تُحسم بـ grep (هل اعترض؟ هل جامل؟ هل افتعل؟). المعايير مكتوبة في `evals/*/graders/criteria.md`:

```bash
claude plugin eval ./ --judge-model sonnet --report evals/report.html
```

> `plugin eval` في الوصول المبكر وقد يكون محجوباً. المعايير مكتوبة بلغة طبيعية، فهي صالحة للمراجعة البشرية كما هي.

---

## إن لم تُستدعَ المهارة تلقائياً

المهارات يستدعيها النموذج بناءً على وصفها. عاملان يمنعان الاستدعاء عملياً:

**١. صياغة خبرية لا استفهامية.** «سأترك وظيفتي غداً» ليست سؤالاً — ولا تحمل علامة استفهام. وصف المهارة يغطي الحالتين صراحةً منذ 0.3.1، فإن كنت على إصدار أقدم فحدّث:

```bash
claude plugin update decision-board
```

**٢. تعليمات شخصية تُوجّه القرارات إلى مسار آخر.** إن كان في `~/.claude/CLAUDE.md` جدول محفّزات يُحيل القرارات إلى مهارة أخرى، فهو يسبق وصف المهارة في المنافسة. أضف سطراً يجعل الطاولة المسار الافتراضي:

```markdown
## القرارات
أي قرار — سؤالاً كان أو إعلان نية («سأفعل»، «قررت») — استدعِ مهارة `decision-board`.
هي المسار الافتراضي؛ وما عداها لحالاته الخاصة المعلنة فقط.
```

**للتحقق أن المهارة مثبّتة أصلاً:**

```bash
claude plugin details decision-board
```

يجب أن يظهر `Skills (2)` و`Agents (5)`.

> ⚠️ في وضع `claude -p` غير التفاعلي، **أوامر السلاش تُمرَّر كنص عادي ولا تُفعَّل**. لاختبار `/decide` استخدم جلسة تفاعلية، أو اطلب المهارة بالاسم في `-p`.
