# سجل التغييرات

كل إصدار وما تغيّر فيه ولماذا. القواعد الكاملة في [`PROTOCOL.md`](PROTOCOL.md).

الصيغة مبنية على [Keep a Changelog](https://keepachangelog.com/) والترقيم على [SemVer](https://semver.org/lang/ar/).

---

## [0.5.2] — 2026-08-20

### أُصلح
- **طبقة المفردات في الكاشف كانت تُفعّل على ورود كلمة «قرار» لا على تملّكها.** «أعطيك جميع القرارات والصلاحيات» تفويضٌ لا استشارة، و«راجع قرارات المعمارية» أمر عمل. أُحكمت لتشترط صيغة تملّك أو مواجهة — `قراري` · `أمامي قرار` · `بين خيارين` — وأُضيف استبعاد التفويض وتوجيه العمل (`WORK_DIRECTIVE`).
- **ثالث إيجابية كاذبة تُرصد من رسالة مستخدم حقيقية**، لا من مراجعة نص.

### الاختبارات
- كاشف القرارات: ٥٤ ← **٦٦ حالة** · الفحص البنيوي: ٨٠ ← **٨٢ فحصاً**

---

## [0.5.1] — 2026-08-20

### أُصلح
- **المُتحقِّق كان يسم الغيابات المُعلَنة كوقائع.** «حجم المدخرات غير مذكور» خانة فارغة لا ادعاء يُوسَم — وقد قال الوكيل ذلك بنفسه ثم وسمها. خمسة من ثمانية مُدخلات صارت غيابات.
- **`حرجة: نعم` على كل واقعة** يُلغي الحقل ومعه `الواقعة_الأخطر`. أُضيف سقف ثلاث وقائع حرجة باختبار عملي.

> رُصد **باختبار ميداني للوكيل** على حالة التقييم ٠٧، لا بمراجعة نص.

### أُضيف
- حقل `فراغات_معلنة` منفصل لا يُوسَم · حارسان في `validate.sh`

---

## [0.5.0] — 2026-08-20 — طبقة الإثبات

ج١–ج١٢ تحرس **كيف يُفكَّر**. وحدها لا تكفي: ستة وكلاء منضبطين يستنتجون من وقائع خاطئة يُنتجون خطأً منضبطاً. هذا الإصدار يضيف طبقة تحرس **بماذا يُفكَّر**.

### أُصلح — عيب بنيوي في العزل
- **تناظر التسليح (ج٤/مشتقة ٣).** كان المؤيد يستقبل «سياق المستخدم الكامل» والمعارض لا. ذلك يمنحه وقائع لا يملك خصمه دحضها، ثم يرجّحه الحَكَم بمعيار «الدليل المحدد يسبق العام». **عزلٌ غير متناظر ليس عزلاً بل انحيازاً بوجه إجرائي.**
- `حزمة_المعارض` ← `الحزمة_المحايدة`، ويستقبلها الأربعة بلا فرق حرف. وحقل `الموارد والقدرات` صار المنفذ الوحيد لما يخص صاحب القرار.

### أُضيف — قواعد
- **ج١٣ النظرة من الخارج** — فئة مرجعية إلزامية على كل ادعاء تنبّئي. **رقم بلا مصدر أسوأ من `غير_متاح`**، ويسري على المعارض كما على المؤيد.
- **ج١٤ الوقائع تُفحص لا تُفترض** — وسم كل واقعة، وخفض وزن ما بُني على `مشكوك`.
- **ج١٥ الثقة رقم يُحاسَب عليه** — `0.00–1.00` بسقفين مُعلَنين، وسجل معايرة.

### أُضيف — مكوّنات
- **وكيل سادس `verifier`** على `haiku`، يعمل بالتوازي لا قبل غيره.
- **تشريح فشل مسبق** و**ثلاث عدسات لا تُدمج** (اقتصادية · بشرية وسياسية · زمنية) في المعارض.
- **الدفتر انتقل إلى `~/.claude/decision-board/JOURNAL.md`** — كان في جذر كل مشروع فيتشظّى، فتعود `سابقة_من_الذاكرة` بـ«لا يوجد» دائماً.
- حالتا تقييم لطبقة الإثبات · سقف الفشل · ج٨ صارت تشترط إبطالاً مرصوداً.

### الاختبارات
- الفحص البنيوي: ٥٥ ← **٧٨ فحصاً** · كاشف القرارات: ٢٤ ← **٥٤ حالة** · `smoke.sh`: ٤ ← **٦ حالات**

### كسر توافق
- الاسم `حزمة_المعارض` أُزيل. أي تخصيص محلي يشير إليه يحتاج تحديثاً إلى `الحزمة_المحايدة`.
- الدفتر القديم `.claude/decision-board/MEMORY.md` لم يُهاجَر آلياً — انقله يدوياً إن أردت الاحتفاظ به.

---

## [0.4.0] — 2026-08-19

### أُضيف
- **البلَغن صار مكتفياً بذاته.** hook على `UserPromptSubmit` يكشف صياغة القرار — خبرية أو استفهامية — ويحقن التذكير. لم يعد يعتمد على تعديل موجّه شخصي ليُستدعى، ولا يكلّف سياقاً: يعمل في الهارنس لا في النموذج.
- اختبار وحدة يحرس الكاشف · استبعاد أوامر العمل التقني وتجريد المسارات.

---

## [0.3.1] — 2026-08-19

### أُصلح
- **محفّز الاستدعاء كان يغطي الأسئلة فقط**، فلا تُستدعى المهارة عند إعلان النية («سأترك وظيفتي») — وهو **أخطر شكل للقرار**، لأن غياب علامة الاستفهام يُخفي أن قراراً يُتخذ الآن.

---

## [0.3.0] — 2026-08-19

### أُصلح
- **المستوى ٢ كان يستدعي المعارض وحده** — ميل بنيوي للاعتراض، لا رؤية أوسع. صار مؤيداً ومعارضاً معاً: «لا معارض بلا مؤيد».

### أُضيف
- تنويع النماذج صار مقصوداً وموثّقاً · حالات تقييم لدفتر القرارات.

---

## [0.2.0] — 2026-08-18

### أُصلح
- بنية المستودع · تناقض ج١١ (الوظيفة تُعلَن والاسم يُحجب) · ج٧ صارت سارية في كل المستويات لا في ٣ وحده.

### تغيّر
- **ج٤ صارت بنيوية**: حزمة مغلقة + **بروتوكول رفض التلوث بدل التجاهل**. التجاهل تعليمة، والرفض حجب.

---

## [0.1.0] — 2026-08-18

الإصدار الأول.
